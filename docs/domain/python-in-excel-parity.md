# Python in Excel 대응 상세

마스터 표: 설계서 §1.4 (`docs/pygrid-studio-design.md`). 이 문서는 **구현 중 Excel과 다르게 동작하기로 결정한 항목**을 누적 기록한다.

| # | 항목 | Excel 동작 | PyGrid 동작 | 근거 | 결정일 |
|---|------|-----------|------------|------|--------|
| 1 | 계산 순서 | 뒤쪽 셀 참조 시 이전 값 사용 | `xl()` 의존성을 따라 선행 블록 먼저 실행 | 초보자 직관(참조는 항상 최신 값). 인터뷰 확정 | 2026-09-02 |
| 2 | 코드 위치 | 셀 안 `=PY()` | 우측 Python 패널의 블록 | Python Editor 창과 동일 구조. 설계서 §1.1 | 2026-09-02 |
| 3 | Excel 수식 혼용 | 가능 | v1 미지원(계산은 전부 Python) | 설계서 §1.4 | 2026-09-02 |
| 4 | `xl` 별칭 호출(`f = xl; f("A1")`) | 의존성 추적 불가(동일 한계) | 동일 — ast 분석 누락, 런타임 RuntimeError 안전망 | conversion-rules #14 | 2026-09-03 |
| 5 | XLSX 내보내기의 날짜 | 날짜 셀(서식 포함) | ISO 문자열(값만 정책) — 다시 열면 문자열 | v1 값만 내보내기 트레이드오프. 서식 기록은 v2 | 2026-09-03 |
| 6 | 행/열 삽입 시 `xl()` 참조 | 수식처럼 자동 조정 | v1 미조정 — 코드 문자열 리터럴 유지 | 참조 재작성은 블록 앵커 재배치와 함께 v2 | 2026-09-03 |
| 7 | 결과 위치 변경 | 셀을 잘라내기/붙여넣기하면 `=PY()` 수식이 따라 이동 | 카드의 **출력 위치 지정** → 그리드 클릭으로 앵커 재지정(이전 spill 제거 후 재실행, 한 undo 단계). 코드 안 `xl()` 참조는 여전히 재작성하지 않는다(#6) | 셀 수식이 없어 잘라내기 이동이 불가능 — 명시적 재지정으로 대체 | 2026-09-04 |
| 8 | 출력 선택 | `=PY()`는 마지막 표현식만 반환(다른 값은 코드를 고쳐야 함) | 블록별 **출력**(변수 선택·열 선택·상위 N행)을 코드 수정 없이 지정 | 초보자가 `df.head(3)[["a"]]`를 쓰지 않고도 원하는 부분만 펼치도록. 필터는 §3.3 변환 **전에** 적용 | 2026-09-04 |
| 9 | 문서(마크다운) 블록 | 없음(설명은 셀 텍스트로) | `kind: "markdown"` 블록 — 실행·spill 없음, 앵커는 위치/목차 대상일 뿐 셀에 아무것도 쓰지 않는다. 그리드 앵커에는 `§` 배지(파란색 아님 — Python 관여 아님) | 노트북형 문서화 + 목차. 렌더러는 원시 HTML을 해석하지 않아 공유 파일이 안전하다 | 2026-09-04 |
| 10 | 블록 실행 순서 바꾸기 | 셀 위치를 옮겨야 순서가 바뀜(잘라내기/붙여넣기) | 카드 툴바 ↑↓로 **계산 순서상 이웃과 앵커를 맞바꾼다** — 자리와 실행 순서가 함께 이동(한 undo 단계, 자동 모드는 두 블록 재실행) | 노트북 셀 이동에 대응하는 조작. Excel에는 대응 기능이 없다 | 2026-09-04 |
| 11 | 한 코드의 여러 결과 | `=PY()` 하나 = 결과 하나. 요약표·차트를 따로 놓으려면 셀마다 코드를 복제해 **여러 번 실행** | 블록 하나에 **출력 여러 개**(`outputs[]`) — 코드는 1회 실행하고 결과를 출력마다 다른 위치·모드·선택으로 배치. 출력 하나가 실패해도 나머지는 배치된다 | 같은 코드를 N번 실행하면 부작용·비용이 N배. conversion-rules #20~#23 | 2026-09-04 |

새 항목은 구현 중 발견 즉시 추가한다.

## 부록 E — 참조 뷰(데이터 예제/분석) 이식 시 소스와 달라진 점 (R2·R3, 2026-09-05)

소스(`Actuarial_Platform` /datalab) 대비 변경. 콘텐츠 텍스트(`lib/reference/*`)는 그대로 유지한다.

| # | 항목 | 소스 동작 | PyGrid 동작 | 근거 |
|---|------|-----------|------------|------|
| E1 | 다이얼로그 고정(pin)/PiP | usePinnableDialog(717줄) — 별도 창·드래그 축소창 | shadcn Dialog(max-w 880px, 스크롤)로 간소화. 글자 가−/가+ 배율은 유지, pin/PiP·드래그 제거 | 부록 E.0 확정(간소화). PiP 코드 유지 비용 대비 효용 낮음 |
| E2 | 관리자 override | Supabase `useDatalabOverrides`로 설명 텍스트 덮어쓰기 + ✎ 편집 | 제거 — 정적 데이터 직접 렌더 | 부록 E.0 확정(단절 복사, 서버 없음) |
| E3 | 실행기로 보내기 | 코드를 PyRunner 탭에 적재 | **블록으로 보내기** — 마크다운 제목 블록 + 섹션별 코드 블록 생성(빈 열 자동 앵커, 한 undo 단계) 후 워크북 뷰 전환. 자동 실행 없음. 스니펫 팝업·섹션 단위 보내기도 지원 | 이 앱의 실행기 = 워크북. 부록 E.0 |
| E4 | 뒤로가기로 팝업 닫기 | useHistoryDismiss(pushState) | 제거 — Dialog의 Esc/오버레이 닫기로 충분 | 뷰 전환형 단일 화면이라 history 조작 불필요 |
| E5 | 엑셀 함수 데이터 흐름 도식 | ExcelDataFlowSvg(입력→출력 그림) | 미이식(개념·구문·인수·예제·주의·연관만) | R3 범위 밖. 필요 시 후속 이식 |
| E6 | 실행기 연동 안내 문구 | "'파이썬 코드 실행' 탭 실행기·콤보박스" 언급 | 워크북 블록·'블록으로 보내기' 기준으로 문구 수정(UI 안내문만, 사전 콘텐츠는 원문 유지) | 존재하지 않는 UI를 가리키지 않도록 |
| E7 | 적합 엔진 (R4) | 소스 자체 Pyodide 인스턴스(pyRunner 싱글턴)에서 FIT_SCRIPT 실행 | **이 앱의 워커 런타임** — `runDistributionFit`이 boot→scipy 로드→FS 파일 I/O+exec 래퍼로 실행(사용자 전역 오염 없음). FIT_SCRIPT·수치 방법은 동일 | 런타임 이중화 방지, 부록 E R4 |
| E8 | 분포·적합 팝업 (R4) | 자체 오버레이 모달 + useHistoryDismiss(뒤로가기 닫힘) + DistCodeDialog pin/PiP | 전부 shadcn Dialog(Esc·오버레이 닫힘). 코드 팝업은 공용 CodeDialog — '엑셀 코드 적용' 탭 자동 추가 + **'블록으로 보내기'**(소스에 없던 기능: 분포 코드·적합/시뮬레이션 코드·몬테카를로 코드를 워크북 블록으로) | R2 E1·E4와 동일 규칙 + 워크북 통합 |
| E9 | 적합 데이터 입력 (R4) | 팝업 그리드 직접 입력·클립보드 붙여넣기만 | + **"시트에서 가져오기"** — 현재 그리드 선택 범위(1~3열)를 fitData 파서로 전달(`selectionToCells`) | 이 앱만의 통합(워크북이 데이터 소스) |
| E10 | 데이터 불러오기 (파일/URL/샘플, R5) | Pyodide FS에만 기록 + 로드 셀 자동 추가 | **시트 + 워커 FS 이중 착지** — CSV/XLSX는 새 시트로도 추가(시트명 = 파일명 기반, 중복 시 " (2)"), FS는 같은 이름 덮어쓰기(소스 동일). 로드 블록은 옵션(xl() 참조 기본 / pandas 코드 / 안 함, app settings에 기억)이며 자동 실행 없음. cp949/utf-8-sig 감지는 소스 그대로 이식. 상태 바에 FS 파일 칩(클릭 시 시트 이동 또는 파일명 복사). 드래그 앤 드롭은 기존 '워크북 교체' 동작 유지 — 데이터 추가는 파일 메뉴 경유. **한계**: 현 Pyodide 배포(v314.0.6)에 Excel 엔진(openpyxl 등)이 전무해 `pd.read_excel`은 ImportError — 소스의 ensureExcelSupport(micropip 폴백)에 해당하는 워커 측 지원이 필요(py-runtime 소관). CSV의 `pd.read_csv`는 동작(e2e 검증), xlsx는 xl() 모드 권장 | 시트의 xl() 워크플로와 참조 코드의 pd.read_* 둘 다 그대로 동작해야 한다. 부록 E.0 확정 |
