"use client";

// 모델적합 탭 — R4에서 FitLab(데이터 입력·경험적 패널·적합·결과 표·코드 생성)을 이식한다.

export default function FittingTab() {
  return (
    <section aria-label="모델 적합" className="rounded-lg bg-card p-8 text-center shadow-sm">
      <p className="text-sm text-muted-foreground">모델 적합은 다음 단계에서 제공됩니다.</p>
    </section>
  );
}
