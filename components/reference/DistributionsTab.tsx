"use client";

// 분포 탭 — R4에서 DistributionLab(DistCard·DistChart·비교·VaR/TVaR·QQ)을 이식한다.

export default function DistributionsTab() {
  return (
    <section aria-label="확률분포" className="rounded-lg bg-card p-8 text-center shadow-sm">
      <p className="text-sm text-muted-foreground">
        확률분포 실험실은 다음 단계에서 제공됩니다.
      </p>
    </section>
  );
}
